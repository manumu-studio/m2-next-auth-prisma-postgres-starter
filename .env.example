# ============================================================================
# Database
# ============================================================================
# PostgreSQL connection string (Neon Serverless or local)
# Get your connection string from: https://console.neon.tech
DATABASE_URL=postgres://USER:PASSWORD@HOST/DB?sslmode=require

# ============================================================================
# Auth.js / NextAuth
# ============================================================================
# Application URL (used for OAuth callbacks)
NEXTAUTH_URL=http://localhost:3000

# Secret key for signing JWT tokens (generate with: openssl rand -base64 32)
# Must be at least 32 characters in production
NEXTAUTH_SECRET=change_me_in_prod_min_32_chars

# ============================================================================
# OAuth Providers (Optional - providers only enabled if both ID and SECRET are set)
# ============================================================================

# Google OAuth
# Setup: https://console.cloud.google.com/apis/credentials
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
# Setup: https://github.com/settings/developers
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Facebook OAuth
# Setup: https://developers.facebook.com/
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Apple OAuth (if needed)
APPLE_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

# ============================================================================
# Email Providers
# ============================================================================

# Resend (Recommended)
# Sign up at: https://resend.com/
RESEND_API_KEY=
RESEND_FROM="Manumu Authentication <noreply@manumu.dev>"

# SMTP Fallback (Alternative to Resend)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ============================================================================
# Application
# ============================================================================
# Fallback URL for callback URL assembly (defaults to NEXTAUTH_URL)
APP_URL=http://localhost:3000

# ============================================================================
# Verification Tuning (Optional)
# ============================================================================
# Token expiration time in minutes (default: 30)
VERIFY_TOKEN_TTL_MINUTES=30

# Cooldown period between resend requests in minutes (default: 2)
VERIFY_RESEND_COOLDOWN_MINUTES=2

# Maximum tokens per email address (default: 3)
VERIFY_MAX_TOKENS_PER_EMAIL=3
