# (dashboard) Route Group
Protected area (requires authentication).

- Layout checks `getServerSession(authOptions)` and redirects if unauthenticated.
- Future home of user dashboards or private profile areas.